/*
import 'package:flutter_test/flutter_test.dart';
import 'package:hermoso_store/domain/cubit/global_cubit/cart/cart_cubit.dart';

void main() {
  CartCubit cartCubit = CartCubit();
  group('CartCubitTest', () {

    /// the func that responsible for setup the references and variables.
    setUp(() {
      cartCubit = CartCubit();
    });

    /// the func that responsible for disposing the references and variables.
    tearDown(() {
      cartCubit.close();
    });
  });

  const noTerm = TypeMatcher<CartInitialState>();
  const loading = TypeMatcher<CartLoadingState>();
  const populated = TypeMatcher<CartSuccessState>();
  const error = TypeMatcher<CartErrorState>();
  test(' the initial state for Cart Cubit', () {
    expect(CartInitialState(), throwsA(noTerm));
  });
}
*/
